{{- if .Values.valkey.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "carillon.valkeyFullname" . }}
  labels:
    {{- include "carillon.labels" . | nindent 4 }}
    app.kubernetes.io/component: valkey
spec:
  selector:
    {{- include "carillon.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: valkey
  ports:
    - port: 6379
      targetPort: valkey
{{- end }}
